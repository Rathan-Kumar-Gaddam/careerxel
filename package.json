{
  "name": "careerxel-platform",
  "private": true,
  "scripts": {
    "frontend": "npm --prefix frontend run dev",
    "backend": "npm --prefix backend run develop",
    "dev": "concurrently \"npm --prefix backend run develop\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
